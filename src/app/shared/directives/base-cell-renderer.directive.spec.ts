import { BaseCellRendererDirective } from './base-cell-renderer.directive';

describe('BaseCellRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseCellRendererDirective();
    expect(directive).toBeTruthy();
  });
});
