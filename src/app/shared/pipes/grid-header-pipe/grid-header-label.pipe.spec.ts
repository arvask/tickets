import { GridHeaderLabelPipe } from './grid-header-label.pipe';

describe('GridHeaderLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new GridHeaderLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
