import { GetCellRendererPipe } from './get-cell-renderer.pipe';

describe('GetCellRendererPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCellRendererPipe();
    expect(pipe).toBeTruthy();
  });
});
